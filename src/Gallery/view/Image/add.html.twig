{% extends "layout.html.twig" %}

{% block content %}

  <form role="form" method="post">
    <div class="form-group">
      {{ form_row(form.name) }}
    </div>
    <div class="form-group">
      {{ form_row(form.type_id) }}
    </div>
    {{ form_widget(form) }}
  </form>

{% endblock %}